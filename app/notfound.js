export default function NotFound(){
    return(
        <main>
            <h1 className="text-[#ddd6cb]">Meal Not Found!</h1>
            <p>Unfortunately, no such page / meal was found!</p>
        </main>
    )
}